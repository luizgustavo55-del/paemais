import { View, Text } from 'react-native';

export default function MeuMapa() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>O mapa ainda não está disponível na versão Web.</Text>
    </View>
  );
}
