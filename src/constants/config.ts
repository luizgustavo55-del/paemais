// constants/config.ts

// Mude para 'local' quando estiver programando no Termux.
// Mude para 'nuvem' antes de rodar o EAS Build para gerar o APK.
export const AMBIENTE: 'local' | 'nuvem' = 'nuvem';
